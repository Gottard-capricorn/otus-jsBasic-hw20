test('У функции в прототипе 1 метод?', () => {})

test('Метод вернул другой метод (функцию) или объект, был вызван с тем или иным контекстом', () => {})

test('Конструктор имеет значение Name, Age и т.д.', () => {})

test('В конце отработки вернул объект со всеми заполненными полями?', () => {})