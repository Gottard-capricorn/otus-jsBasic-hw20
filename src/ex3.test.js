//Проверяем, что возвращается прототип объекта в обоих случаях
//Проверяем, что возвращается объект 
//Проверяем, что переданные параметры действительно заполняются в объект
    //Можно ещё на кол-во проверять 

//+ Серегину статью + его заметки наконец импортировать
test('test', () => {
    
})